#!/usr/bin/env bash
# Back-compat wrapper: 'lesson' aliases 'ctf open'.
set -euo pipefail
if [[ $# -eq 0 ]]; then exec ctf open 1; fi
case $1 in
  menu) exec ctf open 1 ;;
  *) exec ctf open "$1" ;;
esac
