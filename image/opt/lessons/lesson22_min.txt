Lesson 22 — SSH & remote access
  Create ~/.ssh/id_ctf22, install the public key into ctf22's
  authorized_keys via sudo, then ssh ctf22@localhost and read
  /home/ctf22/flag.txt.
Submit: ctf submit 22 FLAG{...}
